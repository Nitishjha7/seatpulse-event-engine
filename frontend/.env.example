# Copy this to create ".env". .env is ignored by Git, but this file is tracked.
#   PowerShell:  Copy-Item .env.example .env
#   Git Bash:    cp .env.example .env

# In Vite, only variables prefixed with VITE_ are exposed to the frontend.
VITE_API_URL=http://localhost:8000
