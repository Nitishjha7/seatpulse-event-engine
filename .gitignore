# ---------- Python / Backend ----------
__pycache__/
*.py[cod]
*.egg-info/
.Python
venv/
env/
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
*.db
*.sqlite3

# ---------- Node / Frontend ----------
node_modules/
dist/
dist-ssr/
build/
.vite/
*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ---------- Environment / Secrets ----------
# NEVER commit these — they contain passwords and API keys
.env
.env.*
!.env.example
*.pem
*.key

# ---------- Docker ----------
docker-compose.override.yml

# ---------- Personal Notes ----------
# Setup guides and command references — for local reference only,
# Do not push to GitHub

# ---------- Editors / OS ----------
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db
desktop.ini

# ---------- Logs ----------
logs/
*.log
tickets/

# Benchmark CSV outputs — generated on every run.
# Official results are documented in documents/phases/15-locking-benchmark.md.
loadtest/results/

# Git Bash on Windows translates paths like `/tmp/...` and `/w` inside the project,
# creating invalid directories like `frontend/C:/...` or `backend;W`.
#
#
# Therefore: create build/temp files inside the container, not on the host.
*;W
*;C
**/C:/
_patch.py
_proof.py
